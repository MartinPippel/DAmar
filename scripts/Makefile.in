
include ../Makefile.settings

ALL =
SCRIPTS = rid2bid.py DBprepare.py paths2rids.py marvelStats.sh n50.py splitDiploidAssembly.py \
          createAndSubmitMarvelSlurmJobs.sh	createCommandPlan.sh createCorrectionPlans.sh createFilteringPlans.sh \
          createReadPatchingPlans.sh createRepmaskPlans.sh createScrubbingPlans.sh createSubmitScripts.sh createTouringPlans.sh \
          marvel_slurm.sh slurmStats.sh assemblyStats.sh

all: $(ALL)

install: all
	$(INSTALL_PROGRAM) -m 0755 $(SCRIPTS) $(install_scripts)

clean:

